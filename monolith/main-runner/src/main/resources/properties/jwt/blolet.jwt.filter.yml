blolet:
  jwt:
    filter:
      exclude-paths:
        - /actuator/health
        - /auth/signup
        - /auth/login
        - /auth/email/verification/send
        - /auth/email/verification/check
        - /auth/token/refresh
        - /swagger-ui
