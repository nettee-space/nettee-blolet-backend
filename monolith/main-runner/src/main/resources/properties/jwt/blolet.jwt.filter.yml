blolet:
  jwt:
    filter:
      exclude-paths:
        - /actuator/health
        # Docs 등 static
        - /swagger-ui/**
        - /v3/api-docs/**
        - /auth/signup
        - /auth/login
        - /auth/email/verification/send
        - /auth/email/verification/check
        - /auth/token/refresh
        - /swagger-ui
